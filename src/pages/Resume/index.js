import React from "react";

function Resume() {
  return (
    <section class="resume" id="resume">
            <div class="section-title">
                <h2>Resume</h2>
            </div>
            <div class="section-content">
                <p>
                    this would be where my resume went if i had one. I'm fairly confident in frontend work and I've been working an internship where I primarily use React so that is one of my strong suits as well
                </p>
            </div>
        </section>
  );
}

export default Resume;
